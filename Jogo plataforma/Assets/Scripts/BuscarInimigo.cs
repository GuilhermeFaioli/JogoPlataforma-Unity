using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BuscarInimigo : MonoBehaviour {

    /*public int distanciaInimigo;
    public int contadorInimigos;

	// Use this for initialization
	void Start () {
        contadorInimigos = 5;
        verificaInimigos();

        for (int i=0; i <= 5; i++)
        {
            distanciaInimigo = Random.Range(1, 10);
            Debug.Log("Distancia: "+ distanciaInimigo);
            if (distanciaInimigo < 2)
            {
                Debug.Log("Cuidado");
            }
        }
	}
	
	// Update is called once per frame
	void Update () {
		
	}

    void verificaInimigos()
    {
        while (contadorInimigos>0)
        {
            Debug.Log("Inimigo verificado");
            contadorInimigos--;
        }
    }*/
}
